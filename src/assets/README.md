# Assets Directory

Place your profile image here as `anamta-profile.jpg`.

Once the image file is added:
1. Uncomment the import statement in `src/components/Hero.tsx`
2. Replace the placeholder div with the img tag (see TODO comment in Hero.tsx)
